<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme - CHANGED to Light only, not DayNight -->
    <style name="Theme.AnayaQRScanner" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>

        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>

        <!-- Background colors -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:colorBackground">@color/white</item>

        <!-- Button styles -->
        <item name="materialButtonStyle">@style/Widget.AnayaQRScanner.Button</item>
        <item name="borderlessButtonStyle">@style/Widget.AnayaQRScanner.Button.Borderless</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">?attr/colorPrimaryDark</item>
    </style>

    <!-- Custom Button Style -->
    <style name="Widget.AnayaQRScanner.Button" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/purple_500</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <style name="Widget.AnayaQRScanner.Button.Borderless" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/purple_500</item>
        <item name="backgroundTint">@android:color/transparent</item>
    </style>

    <style name="Widget.AnayaQRScanner.Button.Disabled" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/button_text_disabled</item>
        <item name="backgroundTint">@color/button_background_disabled</item>
        <item name="android:alpha">0.6</item>
    </style>

    <style name="Widget.AnayaQRScanner.Button.Outlined.Disabled" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:textColor">@color/button_text_disabled</item>
        <item name="strokeColor">@color/button_stroke_disabled</item>
        <item name="android:alpha">0.6</item>
    </style>


       <!-- Base application theme -->
        <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
            <item name="colorPrimary">@color/primary_green</item>
            <item name="colorPrimaryDark">@color/primary_dark_green</item>
            <item name="colorAccent">@color/accent_green</item>
            <item name="android:windowBackground">@color/background</item>
            <item name="android:statusBarColor">@color/primary_dark_green</item>
            <item name="android:navigationBarColor">@color/background</item>
        </style>

        <!-- Custom Toolbar Style -->
        <style name="ToolbarStyle" parent="Widget.MaterialComponents.Toolbar.Primary">
            <item name="titleTextColor">@color/text_on_green</item>
            <item name="android:background">@color/primary_green</item>
        </style>

        <!-- Button Styles -->
        <style name="PrimaryButton" parent="Widget.MaterialComponents.Button">
            <item name="backgroundTint">@color/primary_green</item>
            <item name="android:textColor">@color/text_on_green</item>
            <item name="cornerRadius">8dp</item>
        </style>

        <!-- Card Style -->
        <style name="CardStyle" parent="Widget.MaterialComponents.CardView">
            <item name="cardCornerRadius">12dp</item>
            <item name="cardElevation">4dp</item>
            <item name="cardBackgroundColor">@color/background_card</item>
        </style>

</resources>